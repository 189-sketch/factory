Inspect this repository and identify the most complex production-code path that
can be made easier to understand without changing its behavior. Produce an
implementation plan for that improvement.

Work only in a new Git worktree and a new task branch. Never change or push the
repository's default branch. Inspect the real implementation and tests before
deciding what should change. Do not edit product code. Return a concise plan
with objective acceptance checks, important risks, and the worktree and branch
you created.
