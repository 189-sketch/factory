Assess whether the following task is specific, feasible, and ready to build:

<task>
{{factory.task}}
</task>

Inspect the repository before deciding. Identify missing acceptance criteria,
important ambiguity, dependencies, and risks. If the task is ready, produce a
concise implementation plan with objective acceptance checks.

Work only in a new Git worktree and a new task branch. Never change or push the
repository's default branch. Inspect the real implementation and tests before
deciding what should change. Do not edit product code. Return a clear ready or
not-ready verdict, your evidence, and the worktree and branch you created.
